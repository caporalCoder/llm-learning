# Projects

Bigger builds that span weeks. Each gets its own folder with a real README (design → results).

- [`nanoserve/`](nanoserve/) — a small from-scratch LLM inference engine, built to understand serving internals (KV cache, batching, quantization) by making one.
